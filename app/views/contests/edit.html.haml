= content_for :title,
  "#{club_name} - #{t('actions.edit')} #{t('activerecord.models.contest')}"
%h2= "#{t('activerecord.models.contest').capitalize} #{@contest.name}"

= render partial: 'form', locals: {edit: true}
