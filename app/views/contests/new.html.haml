= content_for :title, "#{t('club_name')} - #{t('actions.new')} #{t('activerecord.models.contest')}"
.row
  .col-md-12
    %h2= [t('actions.new'), t('activerecord.models.contest')].join(' ')
%hr
.row
  .col-md-6.col-md-offset-3
    = render 'form'
