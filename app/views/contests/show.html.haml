= content_for :title, "#{t('club_name')} - #{t('activerecord.models.contest')}"
%p
  %strong= "#{t('activerecord.attributes.contest.name')}:"
  = @contest.name

%p
  %strong= "#{t('activerecord.attributes.contest.date')}:"
  = @contest.date
