= content_for :title, "#{t('club_name')} - #{t('actions.new')} #{t('activerecord.models.payment')}"
= javascript_include_tag "payment_new"
.page-header
  %h1= [t('actions.new'), t('activerecord.models.payment')].join(' ')
= render 'form'
